{
  "name": "pnpm-workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preinstall": "npx only-allow pnpm",
    "client": "pnpm --filter client",
    "client:dev": "pnpm --filter client dev",
    "mui-lib": "pnpm --filter mui-lib",
    "mui-lib:dev": "pnpm --filter mui-lib dev",
    "mui-storybook": "NODE_OPTIONS=--openssl-legacy-provider pnpm --filter mui-lib run storybook"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.8.8"
  }
}